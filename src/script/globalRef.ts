import { createRef } from "react";

export const by_name = createRef<HTMLInputElement>();
export const by_city = createRef<HTMLInputElement>();
export const by_dist_lat = createRef<HTMLInputElement>();
export const by_dist_lon = createRef<HTMLInputElement>();
export const by_state = createRef<HTMLInputElement>();
export const by_postal_code = createRef<HTMLInputElement>();
export const by_type = createRef<HTMLSelectElement>();
export const by_country = createRef<HTMLInputElement>();
export const free_search = createRef<HTMLInputElement>();